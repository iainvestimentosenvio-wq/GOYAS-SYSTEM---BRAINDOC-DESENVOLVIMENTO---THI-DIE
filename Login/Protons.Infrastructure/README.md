# Protons.Infrastructure

Camada de persistencia do modulo Login (SQLite/Postgres e repositorios).

## Estrutura principal
- `Login/banco_de_dados/`
- `Login/repositorios/`

## Documentacao
Docs espelhadas em `Login/documentos/doc_login/Login/Protons.Infrastructure/`.

## Build
```bash
cd "Login"
dotnet build Protons.Infrastructure/Protons.Infrastructure.csproj -c Release
```
