# Protons.Core

Camada de dominio e servicos do modulo Login.

## Estrutura principal
- `Login/servicos/`
- `Login/repositorios/` (interfaces)
- `Login/modelos/`
- `Login/validacao/`

## Documentacao
Docs espelhadas em `Login/documentos/doc_login/Login/Protons.Core/`.

## Build
```bash
cd "Login"
dotnet build Protons.Core/Protons.Core.csproj -c Release
```
